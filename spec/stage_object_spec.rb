require 'spec_helper'

describe StageObject do
  it 'has a version number' do
    expect(StageObject::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
